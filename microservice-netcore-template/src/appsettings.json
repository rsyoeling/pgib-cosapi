{
  "Meta": {
    "Env": "$(META_ENV)",
    "foo": "bar",
    "AppName": "$(META_APP_NAME)",
    "ApiKey": "$(META_API_KEY)",
    "LogPath": "$(META_LOG_PATH)",
    "LogBaseLevel": "$(META_LOG_BASE_LEVEL)",
    "LogMicrosoftLevel": "$(META_LOG_MICROSOFT_LEVEL)"
  },
  "Database": {
    "OsilDatabase": {
      "Engine": "Oracle",
      "Host": "$(DATABASE_ORACLE_HOST)",
      "Port": "$(DATABASE_ORACLE_PORT)",
      "Password": "$(DATABASE_ORACLE_PASSWORD)",
      "User": "$(DATABASE_ORACLE_USER)",
      "ServiceName": "$(DATABASE_ORACLE_SERVICE_NAME)",
      "enablePool": "$(DATABASE_POOL_ENABLE)",
      "minPoolSize": "$(DATABASE_POOL_MIN_POOL_SIZE)",
      "maxPoolSize": "$(DATABASE_POOL_MAX_POOL_SIZE)",
      "incrPoolSize": "$(DATABASE_POOL_INCR_POOL_SIZE)",
      "decrPoolSize": "$(DATABASE_POOL_DECR_POOL_SIZE)",
      "connPoolTimeout": "$(DATABASE_POOL_CONNECTION_TIMEOUT)",
      "connPoolLifetime": "$(DATABASE_POOL_CONNECTION_LIFETIME)"
    }
  },
  "Api.Schedulers.SimpleJob1": {
    "CronExpression": "0/15 * * ? * * *",
    "Enabled": "true"
  },
  "Foo": "Im the foo"
}