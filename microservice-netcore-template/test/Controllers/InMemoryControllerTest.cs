using Api;
using Api.Controllers;
using Microsoft.Extensions.Logging;
using Moq;
using System;
using System.Collections.Generic;
using Xunit;

public class InMemoryControllerTest
{
    [Fact]
    public void ReturnsAViewResult_WithAListOfBrainstormSessions()
    {
        // Arrange
        /*var mock = new Mock<ILogger<InMemoryController>>();
        ILogger<InMemoryController> logger = mock.Object;
        var controller = new InMemoryController(logger);

        // Act
        IEnumerable<WeatherForecast> result = controller.Get();
        int num = 0;
        using (IEnumerator<WeatherForecast> enumerator = result.GetEnumerator())
        {
            while (enumerator.MoveNext())
            {
                num++;
            }
        }
        Assert.True(num > 0, "Expected result should be greater than 0.");*/
    }
}
